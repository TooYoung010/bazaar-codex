# 2026-06-02

## 大巴扎图鉴 v0.2 完成（中文 + 图片 + 牌组改名）

**核心突破**：发现 `bazaarplusplus.com` 用 `https://bpp-static.bazaarplusplus.com/card_dict_with_url.json`
作为中文字典源（610KB / 2636 条记录），按物品 ID 匹配，**926 个物品 100% 命中官方简体中文名**。

**图片 CDN 规则**：`https://howbazaar-images.b-cdn.net/images/items/{id}.avif`（CDN 不响应 HEAD，只能 GET）。

## v0.3 完成（牌组库 + 排位查询）

- bazaar-builds.net WordPress REST API：3056 篇牌组
- bazaar.mrmao.life 排位 API：35000 条传奇玩家
- mrmao API quirks：用 `size`（不是 `pageSize`），mainClass 编码 v/d/p/m/j/s/k + `未知`
- Next.js API route `/api/ranking/search` 做服务端代理 + ISR 缓存

## v0.4 完成（修复 Karnok 数据缺失）

howbazaar.gg 里 Karnok 只有 1 个物品（Jerky），从 bazaar-builds.net 各英雄分类的 3056 篇牌组反推 102 个 Karnok 物品 + 15 个其他英雄遗漏物品。

## v0.5 完成（整合 bazaarforge + 双源融合）

**重大发现**：`bazaarforge.gg` 用 Supabase 后端，**anon key 在 JS bundle 里完全暴露**：
- URL: `https://cwlgghqlqvpbmfuvkvle.supabase.co`
- 表：items, builds, heroes, news_posts, changelogs（公开可读）
- builds 表 3114 套，最新到 2026-06-01（活跃！）
- 97% builds 有结构化的 `item_ids`（UUID）

合并后 6156 套牌组（bazaar-builds 历史 + bazaarforge 活跃），Karnok 281 套（vs 之前 181）。

## v0.6 完成（新闻聚合 + 部署准备）

**Steam 官方新闻 API（最权威）**：
```
https://api.steampowered.com/ISteamNews/GetNewsForApp/v0002/?appid=1617400
```
The Bazaar 在 Steam 的 appid = 1617400，可拉最新 20 条公告（含 Patch 14.0 等），含 BBCode 格式 `contents` 字段。

**新闻页架构**：
- 三源融合：Steam 官方（实时，1h ISR）+ bazaar-builds News 分类 51 篇 + bazaarforge changelogs 14 条
- BBCode → HTML 转换器（手写，处理 [h3]/[b]/[list]/[*]/[img]/[url] 等）
- 列表页 + 详情页，新闻正文 CSS 美化（`.news-body`）
- 导航栏加"新闻"页签

**部署准备**：
- git 已初始化（86 个文件，7085 行已 commit）
- 写好 `DEPLOY.md` 部署指引（GitHub 创仓 → push → Vercel 导入，3 步）
- 用户 GitHub: TooYoung010，仓库名 bazaar-codex
- gitignore 已排除日志、node_modules、data/raw、.next

**等待舰长操作**：在 GitHub 创建空仓库 `bazaar-codex`，然后跑 3 行 git 命令推送，最后到 vercel.com/new 导入即可。
